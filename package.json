{
  "private": true,
  "scripts": {
    "build:web": "esbuild web/app.js --bundle --format=esm --minify --outfile=.web-build/app.js"
  },
  "devDependencies": {
    "@noble/hashes": "1.8.0",
    "esbuild": "^0.28.0",
    "esptool-js": "0.6.1",
    "fflate": "0.8.2",
    "svgo": "^3.3.2"
  }
}
